{
  "name": "unleash-v7",
  "version": "7.5.1",
  "description": "NAIS Unleash v7 server with custom authentication",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run --coverage",
    "test:verbose": "vitest run --coverage --reporter=verbose",
    "lint": "eslint 'src/**/*.ts'"
  },
  "dependencies": {
    "@nais/unleash-shared": "workspace:*",
    "unleash-server": "7.5.1"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "@types/supertest": "^7.2.0",
    "nock": "^15.0.0",
    "supertest": "^7.2.2"
  }
}
