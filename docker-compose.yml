version: '3'
services:
  unleash:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - "postgres"
    environment:
      - DISABLE_AUTH=true
      - UNLEASH_PG_USERNAME=unleash
      - UNLEASH_PG_PASSWORD=unleash
      - UNLEASH_PG_URL=postgres://postgres/unleash
  postgres:
    image: "postgres:9"
    environment:
      - POSTGRES_USER=unleash
      - POSTHRES_PASSWORD=unleash
    volumes:
      - ./.data:/var/lib/postgresql/data
