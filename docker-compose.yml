version: "3"
services:
  unleash:
    build: .
    ports:
      - "8080:8080"
    networks:
      - unleash
    depends_on:
      - "postgres"
    environment:
      - DATABASE_USERNAME=unleash
      - DATABASE_PASSWORD=unleash
      - DATABASE_NAME=unleash
      - DATABASE_HOST=postgres
      - DATABASE_SSL=false
      - GOOGLE_PROJECT_ID=nais-management-7178
  postgres:
    image: "postgres:14"
    environment:
      - POSTGRES_USER=unleash
      - POSTGRES_PASSWORD=unleash
    ports:
      - "5432:5432"
    networks:
      - unleash
    #volumes:
    #  - ./.data:/var/lib/postgresql/data

networks:
  unleash:
    driver: bridge
